{
	"name": "remark-validate-href",
	"description": "A remark-cli plugin to validate that links to files and images aren't broken.",
	"version": "0.0.0",
	"types": "./dist/index.d.ts",
	"exports": {
		".": "./dist/index.mjs"
	},
	"license": "MIT",
	"scripts": {
		"build": "tsup",
		"dev": "tsup --watch",
		"lint": "eslint *.ts*",
		"test": "vitest run",
		"coverage": "vitest run --coverage"
	},
	"devDependencies": {
		"@types/node": "^17.0.12",
		"@vitest/coverage-c8": "^0.29.2",
		"eslint": "7.32.0",
		"eslint-config-fennel": "workspace:*",
		"tsconfig": "workspace:*",
		"tsup": "^6.6.3",
		"typescript": "^4.5.3",
		"vitest": "^0.29.2"
	}
}