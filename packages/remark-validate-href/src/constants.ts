import path from 'node:path';

export const CONTENT_BASE = path.join(process.cwd(), 'apps', 'docs', '_content', 'pages');
export const ASSET_BASE = path.join(process.cwd(), 'apps', 'docs', '_content', 'assets');